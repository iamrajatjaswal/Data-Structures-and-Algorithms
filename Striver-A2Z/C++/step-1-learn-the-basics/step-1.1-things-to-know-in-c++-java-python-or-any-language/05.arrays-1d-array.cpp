#include <bits/stdc++.h>
using namespace std;

int main() {
  /*
    1D array
  */
  // int arr[5];
  double arr[5];
  cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];

  // arr[3] += 10;B
  cout << arr[3];

  return 0;
}

// Array elements can be stored in the consecutive memory locations but I am not
// sure where the first one is at and it can be at any randomize location.